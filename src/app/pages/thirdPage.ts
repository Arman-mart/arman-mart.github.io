const Third = {
    render: async () : Promise<string> => {
        const posts = null;
        const content = `
            <div>Third Page</div>
        `
        return content
    },
}

export default Third