const Second = {
    render: async (): Promise <string> => {
        const posts = null;
        const content = `
            <div>Second Page</div>
        `
        return content
    },
}

export default Second